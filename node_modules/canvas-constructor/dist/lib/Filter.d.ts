/// <reference lib="dom" />
import type { BaseCanvas } from './BaseCanvas';
/**
 * Invert an image
 * @param canvas The Canvas instance
 */
export declare const invert: (canvas: BaseCanvas) => BaseCanvas<HTMLCanvasElement, CanvasRenderingContext2D, CanvasImageSource, TextMetrics>;
/**
 * Greyscale an image
 * @param canvas The Canvas instance
 */
export declare const greyscale: (canvas: BaseCanvas) => BaseCanvas<HTMLCanvasElement, CanvasRenderingContext2D, CanvasImageSource, TextMetrics>;
export declare const grayscale: (canvas: BaseCanvas) => BaseCanvas<HTMLCanvasElement, CanvasRenderingContext2D, CanvasImageSource, TextMetrics>;
/**
 * Invert then greyscale an image
 * @param canvas The Canvas instance
 */
export declare const invertGrayscale: (canvas: BaseCanvas) => BaseCanvas<HTMLCanvasElement, CanvasRenderingContext2D, CanvasImageSource, TextMetrics>;
export declare const invertGreyscale: (canvas: BaseCanvas) => BaseCanvas<HTMLCanvasElement, CanvasRenderingContext2D, CanvasImageSource, TextMetrics>;
/**
 * Give an image a sepia tone
 * @param canvas The Canvas instance
 */
export declare const sepia: (canvas: BaseCanvas) => BaseCanvas;
/**
 * Turn an image into a silhouette
 * @param canvas The Canvas instance
 */
export declare const silhouette: (canvas: BaseCanvas) => BaseCanvas;
/**
 * Apply a threshold to the image
 * @param canvas The Canvas instance
 * @param threshold The threshold to apply in a range of 0 to 255
 */
export declare const threshold: (canvas: BaseCanvas, threshold: number) => BaseCanvas;
/**
 * Apply an inverted threshold to the image
 * @param canvas The Canvas instance
 * @param threshold The threshold to apply in a range of 0 to 255
 */
export declare const invertedThreshold: (canvas: BaseCanvas, threshold: number) => BaseCanvas;
/**
 * Brighten an image
 * @param canvas The Canvas instance
 * @param brightness The brightness to apply in a range of 0 to 255
 */
export declare const brightness: (canvas: BaseCanvas, brightness: number) => BaseCanvas;
/**
 * Darken an image
 * @param canvas The Canvas instance
 * @param darkness The darkness to apply in a range of 0 to 255
 */
export declare const darkness: (canvas: BaseCanvas, darkness: number) => BaseCanvas;
export declare const myOldFriend: (canvas: BaseCanvas, darkness: number) => BaseCanvas;
/**
 * Convolute a image. This filter needs a fix.
 * @param canvas The Canvas instance
 * @param weights The weights
 * @param opaque Whether or not pixels should try to be opaque
 * @see https://www.html5rocks.com/en/tutorials/canvas/imagefilters/
 */
export declare const convolute: (canvas: BaseCanvas, weights: readonly number[], opaque?: boolean) => BaseCanvas;
/**
 * Display an image's edges
 * @param canvas The Canvas instance
 */
export declare const edge: (canvas: BaseCanvas) => BaseCanvas;
/**
 * Sharpen an image
 * @param canvas The Canvas instance
 * @param passes The amount of iterations to do
 */
export declare const sharpen: (canvas: BaseCanvas, passes?: number) => BaseCanvas;
/**
 * Blur an image
 * @param canvas The Canvas instance
 * @param passes The amount of iterations to do
 */
export declare const blur: (canvas: BaseCanvas, passes?: number) => BaseCanvas;
//# sourceMappingURL=Filter.d.ts.map